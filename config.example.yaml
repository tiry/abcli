# Agent Builder CLI Configuration
# Copy this file to config.yaml and fill in your values

# HxP Environment ID (required)
environment_id: "your-environment-id"

# OAuth2 Authentication (required)
# You can also set these via environment variables:
# AB_CLIENT_ID and AB_CLIENT_SECRET
client_id: "your-client-id"
client_secret: "your-client-secret"

# API Endpoints (optional - defaults shown)
# Production: https://api.agentbuilder.experience.hyland.com/
# Development: https://api.agentbuilder.dev.experience.hyland.com/
api_endpoint: "https://api.agentbuilder.experience.hyland.com/"

# Auth endpoint (optional - defaults shown)
# Production: https://auth.iam.experience.hyland.com/idp/connect/token
# Development: https://auth.iam.dev.experience.hyland.com/idp/connect/token
auth_endpoint: "https://auth.iam.experience.hyland.com/idp/connect/token"

# OAuth2 grant type (optional - default: client_credentials)
# Supported types from IAM:
#   - client_credentials (default, for service accounts)
#   - urn:hyland:params:oauth:grant-type:api-credentials (for API credentials)
# See: https://auth.iam.dev.experience.hyland.com/idp/.well-known/openid-configuration
grant_type: "client_credentials"

# OAuth2 scopes (optional)
auth_scope: ["hxp environment_authorization"]

# HTTP Settings (optional)
timeout: 30.0       # Request timeout in seconds
max_retries: 3      # Maximum retry attempts for transient errors
retry_backoff: 2.0  # Exponential backoff multiplier

# Output preferences (optional)
# Options: table, json, yaml
default_output_format: "table"

# Editor configuration (optional)
# Text editor for interactive editing (e.g., 'ab agents edit' command)
# Uses same fallback logic as git: $VISUAL, $EDITOR, then platform default
# Examples:
#   editor: vim
#   editor: nano
#   editor: code --wait
# editor: "vim"

# Audit settings (optional)
# When enabled, API payloads for create/update operations will be saved
# to the 'audit' folder next to the config file
record_updates: false

# Pagination Configuration (optional)
pagination:
  # Maximum number of server pages to fetch when using client-side filters
  # Higher values allow more complete filtered results but slower responses
  max_filter_pages: 10

# UI Configuration (optional)
ui:
  # Data Provider mode: "direct", "cli", or "mock"
  # - direct: Use API client directly for best performance (recommended, default)
  # - cli: Use CLI commands via subprocess (legacy, slower)
  # - mock: Use mock data for development and testing
  data_provider: "direct"
  
  # Directory containing mock data files (optional)
  # Only used when data_provider is "mock"
  # Default: ab_cli/abui/data
  mock_data_dir: ""

# Configuration Profiles (optional)
# Profiles allow multiple environment configurations in a single file
# Use with: ab --profile dev agents list
# Manage with: ab profiles list | ab profiles show dev
#
# profiles:
#   dev:
#     client_id: "dev-client-id"
#     client_secret: "dev-secret"
#     api_endpoint: "https://api.agentbuilder.dev.experience.hyland.com/"
#     auth_endpoint: "https://auth.iam.dev.experience.hyland.com/idp/connect/token"
#     timeout: 60.0
#   
#   staging:
#     client_id: "staging-client-id"
#     client_secret: "staging-secret"
#     api_endpoint: "https://api.agentbuilder.staging.experience.hyland.com/"
#     max_retries: 5
#   
#   prod:
#     client_id: "prod-client-id"
#     client_secret: "prod-secret"
#     api_endpoint: "https://api.agentbuilder.experience.hyland.com/"
#     timeout: 45.0
#     default_output_format: "json"
