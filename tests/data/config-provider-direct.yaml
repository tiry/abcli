# Test config file with direct data provider
client_id: "test-client-id"
client_secret: "test-client-secret"
api_endpoint: "https://api.test.com"
auth_endpoint: "https://auth.test.com"

ui:
  data_provider: "direct"
